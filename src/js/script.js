

const btn = document.querySelector('#send');

function showModal () {
    if (btn === showModal) {
        console.log(showModal);
    }

    btn.addEventListener('click', () => {
        btn === showModal;
    });
    
}

showModal();